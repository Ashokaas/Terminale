def oui(text):
    if len(text) == 1:
        return text + " "
    return text

def non(text):
    if len(text) == 1:
        return " " + text
    return text

print("""
      ┌───────────┐      ┌───────────┐ ┌───────────┐ ┌───────────┐
      │ {}        │      │ {}        │ │ {}        │ │ {}        │
      │           │      │           │ │           │ │           │
      │           │      │           │ │           │ │           │
      │           │      │           │ │           │ │           │
      │        {} │      │        {} │ │        {} │ │        {} │
      └───────────┘      └───────────┘ └───────────┘ └───────────┘""", end="")

print("""
      ┌───────────┐      ┌───────────┐ ┌───────────┐ ┌───────────┐
      │ {}        │      │ {}        │ │ {}        │ │ {}        │
      │           │      │           │ │           │ │           │
      │           │      │           │ │           │ │           │
      │           │      │           │ │           │ │           │
      │        {} │      │        {} │ │        {} │ │        {} │
      └───────────┘      └───────────┘ └───────────┘ └───────────┘""", end="")

